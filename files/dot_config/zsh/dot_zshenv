export ZDOTDIR=~/.config/zsh

# disable system-zshrc
# SEE: http://fardinshikhiyev.github.io/Speed-up-your-oh-my-zsh/
export skip_global_compinit=1

# ignore /etc/zprofile, /etc/zshrc, /etc/zlogin, and /etc/zlogout
# ref. http://zsh.sourceforge.net/Doc/Release/Files.html
# ref. http://zsh.sourceforge.net/Doc/Release/Options.html#index-GLOBALRCS
unsetopt GLOBAL_RCS

# system-wide environment settings for zsh(1)
if [ -x /usr/libexec/path_helper ]; then
  eval `/usr/libexec/path_helper -s`
fi

if [ -z "$USER" ]; then
  export USER=`id -un`
fi

export path=(
  ~/.local/bin(N-/)
  ~/.local/share/aquaproj-aqua/bin(N-/)
  /usr/local/opt/coreutils/libexec/gnubin(N-/)
  /usr/local/opt/grep/libexec/gnubin(N-/)
  /usr/local/opt/gsed/libexec/gnubin(N-/)
  $path
)
export fpath=(
  ~/.local/share/zsh/functions(N-/)
  ~/.local/share/zsh/vendor-completions(N-/)
  ~/.local/share/zsh/plugins/zsh-completions/src(N-/)
  $fpath
)

export XDG_CONFIG_HOME=~/.config
export XDG_CACHE_HOME=~/.cache
export XDG_DATA_HOME=~/.local/share
export XDG_STATE_HOME=~/.local/state

export CURL_HOME=~/.config/curl

export LESSKEY=~/.config/less/.less

export INPUTRC=~/.config/readline/inputrc

export STARSHIP_CONFIG=~/.config/starship/config.toml

export AQUA_GLOBAL_CONFIG=~/.config/aquaproj-aqua/aqua.yaml

if [ -e ~/.config/zsh/.zshenv.local ]; then
  source ~/.config/zsh/.zshenv.local
fi

typeset -U PATH path FPATH fpath MANPATH manpath
