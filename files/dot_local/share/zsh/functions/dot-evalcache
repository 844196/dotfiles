local EVALCACHE_DIR=~/.cache/zsh/evalcache

local key=$1
if [ -z "${commands[$key]}" ]; then
  return
fi

local cache=$EVALCACHE_DIR/${key}.zsh
if [ -f $cache ]; then
  source $cache
  return
fi

"$@" > $cache
zcompile $cache
source $cache
