local __DIR__ __FILE__
for __DIR__ in ${^fpath}/dot-widgets(N-/); do
  fpath=($__DIR__ $fpath)

  for __FILE__ in $__DIR__/.*(N-.); do
    autoload -Uz -- "${__FILE__:t}"
    zle -N -- "${__FILE__:t}"
  done
done

unset __DIR__ __FILE__
