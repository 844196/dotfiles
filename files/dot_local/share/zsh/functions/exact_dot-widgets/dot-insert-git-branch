local selected="$(
  git branch --color=always -vvv --sort=authordate \
    | dot-fzf --no-exit-0 \
    | grep -o -E '^[ *] \S+' | cut -c3- | tr '\n' ' '
)"

LBUFFER+="$selected"
CURSOR=$#LBUFFER

zle reset-prompt
