# vim: set ft=zsh:

if [[ "$BUFFER" == "b" ]]; then
  BUFFER="git checkout -b $(git config --local user.name)/$(git rev-parse --abbrev-ref HEAD)/"
  CURSOR=${#BUFFER}
else
  zle _expand_alias
  zle .self-insert
fi
