# `files/dot_claude/` (`~/.claude/`)

`~/.claude/CLAUDE.md` は使用しない。トピックごとに `exact_rules/*.md` に分割して管理する。
- `exact_local-marketplace/` — ローカル専用プラグインのマーケットプレイス。
  - 協調する複数 hook を 1 セットでバンドルしたい、構成要素が一つだけだったとしても実装背景を説明するための文書が必要な場合は、プラグインとして切り出す。
