# `files/dot_claude/exact_skills/` (`~/.claude/skills/`)

`~/.claude/skills/` にデプロイされるカスタムスキル定義の参照ポインタ。

## commit / commit-amend / headless-commit / headless-commit-amend

実装の本体は `files/exact_dot_844196/exact_bin/executable_git-claude-commit` を参照。

## fork-tmux / handover-tmux

実装の本体は `files/exact_dot_844196/exact_bin/executable_claude-yolo` を参照。

## claude-code-meta

`references/claude-code-system-prompts/` の中身は `files/.chezmoiexternal.yaml` 経由で `Piebald-AI/claude-code-system-prompts` の tarball を展開している。chezmoi のソースには含まれない (展開先のみ chezmoi が配置)。
