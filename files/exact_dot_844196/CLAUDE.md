# `files/exact_dot_844196/` (`~/.844196/`)

このディレクトリは「`~/.local/bin` は well-known なため `exact_` をつけることが出来ない」問題を回避するために用意されたスクリプト置き場です。

- `./exact_bin/` - `~/.local/bin/` 相当
- `./exact_libexec/` - `~/.local/libexec/` (システムであれば `/usr/local/libexec/`) 相当
